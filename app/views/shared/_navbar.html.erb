<nav class="bg-white shadow-sm flex justify-between items-center px-4 py-2">
  <a href="/" class="text-xl font-bold">Real Ancient History</a>
<div class="flex items-center space-x-4">
  <% if Current.user %>    
    <%= link_to "Create", new_post_path, class: "text-gray-500 hover:text-gray-900" %>
    <%= button_to "Logout", session_path, method: :delete, class: "text-gray-500 hover:text-gray-900" %>
  <% else %>
    <%= link_to "Login", new_session_path, class: "text-gray-500 hover:text-gray-900" %>
  <% end %>
</div>
</nav>